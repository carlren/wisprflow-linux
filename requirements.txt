# core
requests>=2.28
numpy>=1.24
sounddevice>=0.4.6
soundfile>=0.12
# optional but recommended for better hotkey + paste
pynput>=1.7
python-xlib>=0.33; platform_system == "Linux"
# GTK is via system python3-gi, no pip package needed
# AppIndicator via gir1.2-ayatanaappindicator3-0.1
